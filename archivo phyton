def factorial(n):
    if n == 0:  # Caso base
        return 1
    else:  # Caso recursivo
        return n * factorial(n - 1)

if __name__ == "__main__":
    number = 5
    print(f"Factorial de {number} es: {factorial(number)}")
